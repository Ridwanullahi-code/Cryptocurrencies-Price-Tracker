import React from 'react';

function Input() {
  return (
    <div className="h-flex search-container">
      <input
        type="search"
        className="search"
        placeholder="search..."
      />
    </div>
  );
}

export default Input;
